# T25 Compiler (System Programming Course Project)

![Language](https://img.shields.io/badge/language-C%2B%2B-blue.svg)
![Type](https://img.shields.io/badge/type-Coursework-green.svg)

**T25 Compiler** — це програмна система для трансляції навчальної мови програмування, розроблена в рамках курсового проєкту з дисципліни "Системне програмування". Проєкт демонструє практичну реалізацію класичних принципів побудови компіляторів.

## 📄 Опис проєкту

Головна мета проєкту — створення повноцінного транслятора, який перетворює вихідний код високого рівня у машинні інструкції або код цільової мови (C/Assembly). Система реалізує повний цикл обробки програмного коду, включаючи лексичний, синтаксичний та семантичний аналіз, а також генерацію та виконання коду.

Архітектура транслятора побудована за модульним принципом, що дозволяє чітко розмежувати етапи обробки даних та забезпечує гнучкість системи.

## 📋 Специфікація вхідної мови (Варіант 25)

Транслятор працює зі специфічною мовою програмування, яка має наступні характеристики:

* **Типи даних:** Підтримка 32-бітних цілих чисел (`int32`) та одновимірних масивів.
* **Ідентифікатори:** Суворий формат імен змінних — дві великі латинські літери (наприклад, `AB`, `XY`).
* **Оператор присвоєння:** Нестандартний напрямок обчислення **зліва направо** (синтаксис: `Вираз -> Змінна`).
* **Арифметика:** Використання ключових слів для операцій: `add`, `mul`, `div`, `mod` та символьних операторів.
* **Логіка та порівняння:** Підтримка операторів `lt` (менше), `gt` (більше), `==`, `!=`, а також логічних `&`, `|`, `!`.
* **Керуючі конструкції:**
    * Умовний оператор `if ... else` (стиль Rust/Go, без дужок навколо умови).
    * Цикл `while` з обов'язковими блоками у фігурних дужках `{ ... }`.
* **Ввід/Вивід:** Вбудовані оператори для консольної взаємодії.

## 👤 Автор

**Турак Микола**
Група КІ-309
Варіант №25

---
*Проєкт виконано у 2025-2026 навчальному році.*